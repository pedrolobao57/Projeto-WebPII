{
  "name": "parksmart-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:f": "yarn --cwd frontend dev",
    "build:f": "yarn --cwd frontend build",
    "start:b": "yarn --cwd backend start",
    "seed:b": "yarn --cwd backend seed"
  }
}
